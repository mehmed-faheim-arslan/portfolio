{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && gh-pages -d dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "gh-pages": "^6.3.0",
    "html-minifier-terser": "^7.2.0"
  }
}
